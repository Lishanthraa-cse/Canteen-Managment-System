# Allow all bots to crawl everything
User-agent: *
Disallow:

# Allow search engines to index the homepage
Allow: /

# Block crawling of admin and API routes
Disallow: /admin
Disallow: /api

# Sitemap (if you have one)
Sitemap: https://yourwebsite.com/sitemap.xml
